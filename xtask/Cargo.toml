[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false

[package.metadata.release]
# Internal dev tool — cargo-release should never bump or tag it.
release = false

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
clap_mangen = "0.3"
crossterm = "0.29"
ctrlc = "3.5.2"
daft = { path = "..", version = "1.28.0" }
# Real-dir resolution for the `real-state-guard` tripwire. Pinned to the same
# version daft itself uses so it reuses the already-locked entry (no cooldown).
dirs = "6.0"
indicatif = "0.18.4"
rayon = "1.10"
# Reflink + recursive walk for the sandbox's template snapshot / reset.
# Inlined in xtask rather than imported from `daft::cow_copy` because the
# YAML runner port rule in CLAUDE.md forbids `runner.rs / sandbox.rs /
# executor.rs` from naming `daft::`. The duplication is intentional.
reflink-copy = "0.1.29"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
shlex = "2"
tempfile = "3.8"
term-styles = { workspace = true }
walkdir = "2.5"
