.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.TH git-worktree-merge 1  "git-worktree-merge 1.28.0" 
.SH NAME
git\-worktree\-merge \- Merge branches across worktrees
.SH SYNOPSIS
\fBgit\-worktree\-merge\fR [\fB\-\-into\fR] [\fB\-\-abort\fR] [\fB\-\-continue\fR] [\fB\-\-quit\fR] [\fB\-m \fR] [\fB\-F\fR|\fB\-\-file\fR] [\fB\-\-edit\fR] [\fB\-\-no\-edit\fR] [\fB\-\-cleanup\fR] [\fB\-\-merge\fR] [\fB\-\-squash\fR] [\fB\-\-rebase\fR] [\fB\-\-rebase\-merge\fR] [\fB\-\-commit\fR] [\fB\-\-no\-commit\fR] [\fB\-\-signoff\fR] [\fB\-\-no\-signoff\fR] [\fB\-s\fR|\fB\-\-strategy\fR] [\fB\-X\fR|\fB\-\-strategy\-option\fR] [\fB\-S\fR|\fB\-\-gpg\-sign\fR] [\fB\-\-no\-gpg\-sign\fR] [\fB\-\-verify\-signatures\fR] [\fB\-\-no\-verify\-signatures\fR] [\fB\-\-allow\-unrelated\-histories\fR] [\fB\-\-stat\fR] [\fB\-n\fR|\fB\-\-no\-stat\fR] [\fB\-\-adopt\-target\fR] [\fB\-\-no\-adopt\-target\fR] [\fB\-y\fR|\fB\-\-yes\fR] [\fB\-r\fR|\fB\-\-remove\-branch\fR] [\fB\-\-keep\-branch\fR] [\fB\-\-ff\-only\fR] [\fB\-\-no\-ff\-only\fR] [\fB\-\-source\-worktree\fR] [\fB\-\-skip\-hooks\fR] [\fB\-\-hooks\fR] [\fB\-\-skip\-tag\fR] [\fB\-\-only\-tag\fR] [\fB\-\-set\-default\fR] [\fB\-v\fR|\fB\-\-verbose\fR] [\fB\-\-format\fR] [\fB\-\-template\fR] [\fB\-\-no\-headers\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fB\-V\fR|\fB\-\-version\fR] [\fISOURCE_OR_TARGET\fR] 
.SH DESCRIPTION
.PP
Merges one or more source branches into a target worktree\*(Aqs branch.
.PP
When \-\-into is omitted, the target is the current worktree\*(Aqs branch,
mirroring `git merge`. When \-\-into <target> is supplied, the merge is
performed against that worktree\*(Aqs branch from wherever you are.
.PP
Multiple sources invoke git\*(Aqs octopus strategy, announced explicitly.
.PP
Finish commands (\-\-abort, \-\-continue, \-\-quit) take an optional positional
<worktree|branch>; default to the current worktree\*(Aqs branch.
.PP
When post\-merge cleanup deletes a merged branch\*(Aqs remote ref (enabled via
daft.branchDelete.remote), that delete pushes no content, so the repo\*(Aqs
pre\-push hook is skipped by default. Set daft.pushVerify to always for hooks
that gate deletes by ref name; merge has no per\-invocation bypass.
.SH OPTIONS
.TP
\fB\-\-into\fR \fI<TARGET>\fR
Target worktree/branch. Defaults to the current worktree\*(Aqs branch
.TP
\fB\-\-abort\fR
Abort an in\-progress merge in the named worktree (defaults to CWD)
.TP
\fB\-\-continue\fR
Continue an in\-progress merge in the named worktree (defaults to CWD).

When continuing a squash\-staged state, the commit\-message flags (`\-\-no\-edit`, `\-m`, `\-F`, `\-\-signoff`, `\-\-gpg\-sign`, `\-\-cleanup`) may be supplied to control the commit step. They are forwarded to `git commit` and are *not* treated as start\-mode flags.
.TP
\fB\-\-quit\fR
Quit an in\-progress merge without resetting the index (defaults to CWD)
.TP
\fB\-m\fR \fI<MSG>\fR
Commit message for the merge commit (mirrors `git merge \-m`)
.TP
\fB\-F\fR, \fB\-\-file\fR \fI<FILE>\fR
Read the commit message from FILE (mirrors `git merge \-F`)
.TP
\fB\-\-edit\fR
Launch the editor to edit the merge commit message
.TP
\fB\-\-no\-edit\fR
Accept the auto\-generated merge commit message without editing
.TP
\fB\-\-cleanup\fR \fI<MODE>\fR
Message cleanup mode (mirrors `git merge \-\-cleanup`)
.TP
\fB\-\-merge\fR
Explicit merge style — always create a merge commit. This is the default; the flag exists for canceling a config\-set default style
.TP
\fB\-\-squash\fR
Squash style — collapse source\*(Aqs commits into one squashed commit on target
.TP
\fB\-\-rebase\fR
Rebase style — rebase source onto target, then fast\-forward (linear, preserves commits)
.TP
\fB\-\-rebase\-merge\fR
Rebase\-merge style — rebase source onto target, then create a merge commit
.TP
\fB\-\-commit\fR
Automatically create the merge commit after a successful merge
.TP
\fB\-\-no\-commit\fR
Leave the merge staged without committing
.TP
\fB\-\-signoff\fR
Add a Signed\-off\-by trailer to the merge commit
.TP
\fB\-\-no\-signoff\fR
Explicitly disable signoff (cancel a config default)
.TP
\fB\-s\fR, \fB\-\-strategy\fR \fI<STRAT>\fR
Merge strategy to use (e.g. `ours`, `recursive`, `octopus`)
.TP
\fB\-X\fR, \fB\-\-strategy\-option\fR \fI<OPT>\fR
Strategy\-specific option (repeatable; mirrors `git merge \-X`)
.TP
\fB\-S\fR, \fB\-\-gpg\-sign\fR [\fI<KEYID>\fR]
GPG\-sign the merge commit. Accepts an optional KEYID; omit to use the default key
.TP
\fB\-\-no\-gpg\-sign\fR
Do not GPG\-sign the merge commit (cancels `commit.gpgsign` config)
.TP
\fB\-\-verify\-signatures\fR
Verify that the tip commit of the source is signed with a valid key
.TP
\fB\-\-no\-verify\-signatures\fR
Do not verify signatures on the source tip commit
.TP
\fB\-\-allow\-unrelated\-histories\fR
Allow merging histories that share no common ancestor
.TP
\fB\-\-stat\fR
Show a diffstat at the end of the merge
.TP
\fB\-n\fR, \fB\-\-no\-stat\fR
Suppress the diffstat at the end of the merge
.TP
\fB\-\-adopt\-target\fR
When the target has no worktree and the merge is not a pure fast\-forward, create an ephemeral worktree to perform the merge without prompting
.TP
\fB\-\-no\-adopt\-target\fR
When the target has no worktree and the merge is not a pure fast\-forward, refuse without prompting
.TP
\fB\-y\fR, \fB\-\-yes\fR
Auto\-accept interactive prompts. Implies \-\-adopt\-target when neither \-\-adopt\-target nor \-\-no\-adopt\-target is supplied. Future\-proofs any new prompts we add
.TP
\fB\-r\fR, \fB\-\-remove\-branch\fR
Remove the source worktree and delete the source branch. The local/remote behavior follows `branch.deleteRemote` (defaults to local\-only)
.TP
\fB\-\-keep\-branch\fR
Explicit keep — for canceling a config\-set `merge.cleanup = remove\-branch`
.TP
\fB\-\-ff\-only\fR
Require the merge to be fast\-forward\-equivalent: the source must already contain the target\*(Aqs tip. Supplies `merge: ff: only` when daft.yml lacks it; matches it when committed
.TP
\fB\-\-no\-ff\-only\fR
Allow a non\-fast\-forward merge for this invocation, overriding a committed `merge: ff: only` policy (announced when it does)
.TP
\fB\-\-source\-worktree\fR \fI<STATE>\fR
Required state of the source branch\*(Aqs worktree: `clean` refuses a missing or dirty source worktree (supplies `merge: source_worktree: clean`); `any` relaxes a committed `clean` for this invocation
.br

.br
\fIPossible values:\fR
.RS 14
.IP \(bu 2
clean: Require a checked\-out, clean source worktree
.IP \(bu 2
any: No requirement on the source worktree (relaxes a committed `clean` for this invocation)
.RE
.TP
\fB\-\-skip\-hooks\fR \fI<SELECTOR>\fR
Skip hooks this run (all | <hook> | tag:<tag> | <job>); repeatable/comma\-separated
.TP
\fB\-\-hooks\fR \fI<MODE>\fR [default: auto]
How this run\*(Aqs hook jobs execute
.br

.br
\fIPossible values:\fR
.RS 14
.IP \(bu 2
auto: Honor each job\*(Aqs own `background:` declaration — the default
.IP \(bu 2
foreground: Run every job inline and wait for the whole phase to finish
.IP \(bu 2
background: Dispatch every job to the coordinator and return without waiting
.IP \(bu 2
off: Skip the hook phase entirely — equivalent to `\-\-skip\-hooks all`
.RE
.TP
\fB\-\-skip\-tag\fR \fI<TAG>\fR
Skip hook jobs carrying TAG, plus their dependents (repeatable). Sugar for `\-\-skip\-hooks tag:<TAG>` — e.g. `\-\-skip\-tag deep` for a fast gate pass
.TP
\fB\-\-only\-tag\fR \fI<TAG>\fR
Run only hook jobs carrying TAG (repeatable)
.TP
\fB\-\-set\-default\fR
Write the resolved style/cleanup choices to `git config \-\-local` after the merge succeeds
.TP
\fB\-v\fR, \fB\-\-verbose\fR
Be verbose; show detailed progress
.TP
\fB\-\-format\fR \fI<FORMAT>\fR
Output format. Mutually exclusive with \-\-template
.br

.br
\fIPossible values:\fR
.RS 14
.IP \(bu 2
json
.IP \(bu 2
ndjson
.IP \(bu 2
tsv
.IP \(bu 2
csv
.IP \(bu 2
yaml
.IP \(bu 2
toon
.IP \(bu 2
markdown
.RE
.TP
\fB\-\-template\fR \fI<STR>\fR
Tera template string. Mutually exclusive with \-\-format
.TP
\fB\-\-no\-headers\fR
Omit header row (tsv/csv only)
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
\fB\-V\fR, \fB\-\-version\fR
Print version
.TP
[\fISOURCE_OR_TARGET\fR]
Source branches/commits to merge (start mode), OR optional target worktree/branch for \-\-abort / \-\-continue / \-\-quit (finish mode; max one positional)
.SH VERSION
v1.28.0
